apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.common.app.name}}-service
spec:
  selector:
    app: {{ .Values.common.app.name}}-app
  ports:
    {{- range .Values.common.service.ports }}
    - name: {{ .portName }}
      port: {{ .port }}
      targetPort: {{ .targetPort }}
    {{- end }}
  type: LoadBalancer
  externalIPs:
    {{- range .Values.common.service.externalIPs }}
    - {{ . }}
    {{- end }}