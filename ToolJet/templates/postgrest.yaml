{{- if .Values.postgrest.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.postgrest.name}}-service
spec:
  selector:
    app: {{ .Values.postgrest.name}}-app
  template:
    metadata:
      labels:
        app: {{ .Values.postgrest.name}}-app
  ports:
    {{- range .Values.postgrest.ports }}
    - name: {{ .portName }}
      port: {{ .port }}
      targetPort: {{ .targetPort }}
    {{- end }}
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.postgrest.name }}-deployment
  labels:
    app: {{ .Values.postgrest.name }}-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.postgrest.name }}-app
  template:
    metadata:
      labels:
        app: {{ .Values.postgrest.name }}-app
    spec:
      {{- if .Values.postgrest.volumes }}
      volumes: 
      {{- range .Values.postgrest.volumes }}
        - name: {{ .name }}
          persistentVolumeClaim:
            claimName: {{ .pvcName}}
      {{- end }}
      {{- end }}
      containers:
        - name: {{ .Values.postgrest.name }}-app
          image: {{ .Values.postgrest.image }}
          {{- if .Values.postgrest.env }}
          env:
          {{- range .Values.postgrest.env }}
            - name: "{{ .name }}"
              value: "{{ .value }}"
          {{- end }}
          {{- end }}
          {{- if .Values.postgrest.volumes }}
          volumeMounts:
          {{- range .Values.postgrest.volumes }}
            - name: "{{ .name }}"
              mountPath: "{{ .mountPath }}"
              subPath: "{{ .subPath }}"
          {{- end }}
          {{- end }}
          {{- if .Values.postgrest.ports }}
          ports:
          {{- range .Values.postgrest.ports }}
            - name: {{ .portName }}
              containerPort: {{ .targetPort }}
          {{- end }}
          {{- end }}
{{- end }}
