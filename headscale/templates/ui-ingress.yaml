apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.headscale-ui.name }}-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/proxy-body-size: "0"
    cert-manager.io/issuer: lets-encrypt
spec:
  template:
    metadata:
      labels:
        app: {{ .Values.headscale-ui.name }}-service
  tls:
  - hosts:
    {{- range .Values.headscale-ui.ingress.hosts }}
    - {{ . }}
    {{- end}}
    secretName: {{ .Values.headscale-ui.name }}-ingress-tls
  rules:
  # add a rule for each host
    {{- range .Values.headscale-ui.ingress.hosts }}
    - host: {{ . }}
      http:
        paths:
          {{- range $.Values.headscale-ui.service.ports }}
          - path: {{ .path }}
            pathType: Prefix
            backend:
              service:
                name: {{ $.Values.headscale-ui.name }}-service
                port:
                  name: {{ .portName }}
          {{- end }}
    {{- end }}                  